
@{
    ViewData["Title"] = "List";
}

<h1>List</h1>
@if (ViewBag.list.Count > 0)
{
    <ul>
        @foreach (Book book in ViewBag.list)
        {
            <li>@(book.ID) @(book.Title)</li>
        }
    </ul>
}
